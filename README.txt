1) Se utilizo nodejs como lenguaje ya que fue con el lenguaje que mas use en mis trabajos
2) Por temas de tiempos, costos y sencilles, se decidió usar una instancia t2.micro ec2 con docker-compose
3) Al tener que crear solo dos recursos, se decidio hacer la infra a mano en vez de usar IaC
4) Por un tema de seguridad se guarda el apikey en parameter store 
